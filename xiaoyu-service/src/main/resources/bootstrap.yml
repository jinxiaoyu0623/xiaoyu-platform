server:
  port: 1000

spring:
  application:
    name: xiaoyu-service

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

      config:
        file-extension: yaml
        server-addr: localhost:8848

    sentinel:
      transport:
        dashboard: localhost:3000



