server:
  port: 2001

spring:
  application:
    name: xiaoyu-bus

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

      config:
        file-extension: yaml
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
